import 'package:flutter/widgets.dart';

class IntakeH5Frame extends StatelessWidget {
  const IntakeH5Frame({super.key, required this.url});

  final String url;

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
